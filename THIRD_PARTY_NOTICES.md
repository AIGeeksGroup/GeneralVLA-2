# Third-Party Notices

This repository includes or interfaces with third-party research code and
datasets. Keep upstream license headers intact when modifying those files.

Known external dependencies and references include:

- LLaVA and related model code under `GeneralVLA/model/llava/`.
- Segment Anything / SimpleClick style segmentation utilities used by
  `GeneralVLA/evaltools/`.
- REFER/RefCOCO dataset utilities under `GeneralVLA/utils/`.
- mini-swe-agent and WebArena-related code under `KnowledgeBank/`.
- MV-SAM3D, SAM3D, GSO, VGGT-derived, and renderer assets referenced by
  `GeoFuse-MV3D/`.

Large model weights, datasets, benchmark assets, and generated outputs are not
included in this code release. Users should obtain those assets from their
respective official sources and comply with their licenses.
